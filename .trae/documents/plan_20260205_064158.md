Com certeza! Posso restringir o comando `/sitrep` para ser **invisível e inutilizável** para membros comuns, aparecendo apenas para você (Administradores).

### Ajuste de Segurança
-   Usarei a permissão `Administrator` nativa do Discord no comando.
-   Assim, apenas quem tem cargo de Administrador (você) verá o comando no menu `/`.

### Execução do Plano
1.  **Criar Serviço de Notícias (`src/services/news.ts`)**:
    -   Conectar à Steam API.
    -   Filtrar e formatar notícias.
    -   Salvar estado para não repetir.
2.  **Criar Comando Manual (`src/commands/admin/sitrep.ts`)**:
    -   Campos: Título, Mensagem, Imagem, Menção.
    -   **Restrição**: `PermissionFlagsBits.Administrator`.
3.  **Ativar no Bot (`src/events/ready.ts`)**:
    -   Ligar o monitoramento automático ao iniciar.

Vou começar a codar agora!