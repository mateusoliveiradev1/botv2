Aqui está o plano completo e definitivo para finalizar o bot com qualidade "Ultra Premium":

### 1. 🏗️ Infraestrutura e Webhooks (`SetupManager`)
*   **Canal `#conquistas`**:
    *   **Ação**: Criar/Recuperar Webhook chamado `Achievements Feed`.
    *   **Output**: Exibir a URL no terminal para você configurar no Lovable com os eventos (`level_up`, `achievement_unlocked`, etc.).
    *   **Permissão**: Bloquear envio de mensagens (Somente Leitura).
*   **Canal `#regras`**:
    *   **Visual**: Novo Embed "📜 Protocolo de Engajamento" (Estilo Militar, com seções claras).
    *   **Botão Inteligente**: O botão "Alistar-se" verificará se o usuário já possui o cargo. Se sim, avisa "Já alistado" (Ephemeral) para não poluir ou repetir ações.
*   **Canal `#comandos`**:
    *   **Visual**: Novo Embed "💻 Manual de Operações" listando todos os comandos disponíveis.
    *   **Permissão**: Bloquear envio de mensagens (Somente Leitura).
*   **Canais `#sitrep` e `#missoes`**:
    *   **Permissão**: Bloquear envio de mensagens (Somente Leitura).

### 2. ⭐ Sistema de XP do Discord (`XpManager`)
*   **Visual**: Atualizar o Embed de "Level Up" do bot para tentar acompanhar o padrão visual do Lovable (Título "PROMOÇÃO DE CAMPO", cor Dourada, texto formatado).

### 3. 🔐 Privacidade e UX (Comandos)
*   **Modo Fantasma (Ephemeral)**:
    *   Atualizar `/stats`, `/conquistas`, `/passe`, `/nivel`, `/clan` para responderem **apenas para o usuário**.
*   **Limpeza**:
    *   Remover comentários antigos e códigos de teste ("gambiarra") de `conquistas.ts`.

### Resultado Final
O servidor estará visualmente impecável, seguro (canais bloqueados), privado (comandos efêmeros) e pronto para receber o fluxo de dados "Ultra Premium" do Lovable através do webhook configurado.

**Pronto para executar!**