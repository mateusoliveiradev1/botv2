Confirmado:

1.  **Carregamento Automático**: O bot já carrega automaticamente qualquer arquivo novo na pasta `commands`, então `/ajuda` e `/ping` já estão funcionais tecnicamente.
2.  **Atualização Visual**: O comando `/ajuda` já estava listado no manual. O `/ping` ainda não.

### Ação
Vou editar o `SetupManager.ts` para adicionar o `/ping` na lista visual do canal `#comandos`, na seção de Utilitários. Assim fica 100% documentado.

Depois disso, basta rodar `/setup` novamente para atualizar a mensagem.