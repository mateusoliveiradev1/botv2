Com base na análise do código atual, identifiquei **3 lacunas críticas** que ainda faltam para o bot estar 100% completo e operacional conforme prometido no FAQ e nos planos anteriores:

### 1. 🎤 Sistema de XP por Voz (Faltando)
-   **Situação**: O arquivo `faq_knowledge.json` promete "50 XP a cada 10 min em call", e o `constants.ts` tem a constante `VOICE_PER_10MIN`.
-   **Problema**: **Não existe** o arquivo `src/events/voiceStateUpdate.ts`. Ou seja, o bot **não está contando XP de voz** atualmente.
-   **Solução**: Implementar o evento `voiceStateUpdate` para rastrear tempo em call e dar XP periodicamente.

### 2. 📝 Transcrição de Tickets (Faltando)
-   **Situação**: O `TicketManager` tem um comentário `// 1. Log (Transcript placeholder)`.
-   **Problema**: Quando um ticket é fechado, o log apenas diz "Ticket Encerrado". Não salva o histórico da conversa! Isso torna o log de auditoria inútil para provar o que foi dito.
-   **Solução**: Criar um gerador de transcrição (arquivo `.html` ou `.txt`) e anexar ao log no canal `#caixa-preta` ao fechar o ticket.

### 3. 🛡️ Logs de Entrada/Saída (Incompleto)
-   **Situação**: O `guildMemberAdd` dá boas-vindas e cargo, mas **não gera log** no `#caixa-preta`.
-   **Problema**: Não há registro de quem entrou ou saiu no canal de auditoria, apenas no canal de boas-vindas (que é público).
-   **Solução**: Adicionar chamadas ao `LogManager` nos eventos de entrada e criar o evento `guildMemberRemove` (que também não existe) para logar saídas.

### Plano de Ação "Reta Final"
1.  **Criar `voiceStateUpdate.ts`**: Implementar ganho de XP por voz.
2.  **Criar `TranscriptService`**: Gerar arquivo de histórico de ticket.
3.  **Atualizar `TicketManager`**: Anexar o histórico ao fechar.
4.  **Atualizar `guildMemberAdd` e criar `guildMemberRemove`**: Integrar com logs.

Esse é o "missing link" para o bot ser verdadeiramente profissional. Posso executar?