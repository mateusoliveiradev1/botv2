Entendi, temos dois problemas críticos no fluxo de Tickets:

1.  **Erro no botão "Assumir"**: Provavelmente não implementei o handler para o `customId: 'claim_ticket'` no `interactionCreate.ts`.
2.  **Permissão Visual**: O botão aparece para todos, mas deveria ser exclusivo da Staff.
    *   *Limitação do Discord*: Não dá para esconder botões de pessoas específicas na mesma mensagem.
    *   *Solução*: Ao clicar, o bot deve checar se o usuário tem cargo de Staff. Se não tiver, nega com mensagem efêmera. (Visualmente o botão fica lá, mas não funciona para membros comuns).

### Plano de Correção

1.  **Implementar Handler `claim_ticket` (`interactionCreate.ts`)**:
    *   Verificar se quem clicou é Staff (Cargo `🛡️ Task Force Officer` ou `Admin`).
    *   Se for Staff:
        *   Atualizar o Embed do Ticket:
            *   Mudar Status para: `🟢 Em Atendimento por @Staff`.
            *   Mudar Cor para Verde.
        *   Enviar mensagem no chat avisando: "👮‍♂️ **@Staff** assumiu este chamado."
    *   Se NÃO for Staff:
        *   Responder com Ephemeral: "⛔ Acesso Negado. Apenas oficiais podem assumir tickets."

2.  **Refinar `TicketManager.ts`**:
    *   Não vou mexer na criação, pois o botão precisa estar lá para a Staff clicar assim que ver o ticket.

Vou corrigir o `interactionCreate.ts` agora mesmo.