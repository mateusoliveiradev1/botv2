Entendi. Atualmente, a duração no log de encerramento está hardcoded como `'N/A (WIP)'`. Vamos consertar isso calculando o tempo real.

### Plano de Correção: Cálculo de Duração do Ticket

1.  **Capturar Data de Criação**:
    *   Todo canal de texto no Discord tem uma propriedade `createdTimestamp`.
    *   Podemos usar isso para calcular a diferença entre `Agora` e `Criação`.

2.  **Atualizar `TicketManager.ts`**:
    *   No método `closeTicket`, vamos calcular:
        ```typescript
        const durationMs = Date.now() - channel.createdTimestamp;
        // Converter para formato legível (ex: "2h 15m")
        ```
    *   Substituir o `'N/A (WIP)'` pelo valor formatado no Log.

3.  **Utilitário de Tempo**:
    *   Vou criar um helper simples `formatDuration(ms)` para deixar bonito (ex: `1d 2h 30m`).

Vou implementar essa lógica agora mesmo! ⏱️